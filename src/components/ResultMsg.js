//This component shows message after ProgressBar component
//That message depends of what user have written in
import React from 'react'

export const ResultMsg = ({msg}) => {
    return (
        <div>
            {msg}
        </div>
    )
}
